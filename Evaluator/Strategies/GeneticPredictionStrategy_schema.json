{
  "type": "object",
  "title": "GeneticPredictionStrategyEvaluator",
  "properties": {
    "required_time_frames": {
        "type": "array",
        "title": "Analysed time frame: only the first one will be considered for GeneticPredictionStrategy",
        "uniqueItems": true,
        "minItems": 1,
        "maxItems": 1,
        "items": {
            "title": "Time frame",
            "type": "string",
            "default": "12h",
            "enum": ["1m", "3m", "5m", "15m", "30m", "1h", "2h", "4h", "6h", "8h", "12h", "1d", "3d", "1w", "1M"]
        }
    }
  }
}